name: Performance Guardrails

on: [push, pull_request]

jobs:
  lighthouse-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - run: npm install
      - run: npm run build
      
      # تشغيل السيرفر (الجزء الذي نجح سابقاً)
      - name: Start Preview Server
        run: |
          npm run preview -- --port 4173 &
          npx wait-on http://localhost:4173
      
      - name: Lighthouse CI
        uses: treosh/lighthouse-ci-action@v10
        with:
          urls: |
            http://localhost:4173
          uploadArtifacts: false # 🛑 إيقاف الرفع التلقائي المسبب للمشكلة
          temporaryPublicStorage: true
          budgetPath: ./.github/lighthouse-budget.json
          runs: 1
      
      # ✅ استخدام الأداة الرسمية لرفع النتائج
      - name: Upload Lighthouse Results
        uses: actions/upload-artifact@v3
        if: always() # تنفيذ الرفع حتى لو فشل اختبار الأداء
        with:
          name: lighthouse-report
          path: .lighthouseci/
